# STM32CubeIDE
.settings/
.project
.cproject
# ※チーム開発でIDE設定を共有したい場合は.project/.cprojectは含めても良いが、
#   絶対パスが含まれることがあるため基本は除外

# Build artifacts
build/
Debug/
Release/
*.list
*.map

# Generated by CubeMX
**/Middlewares/
**/Drivers/
# ※これらを無視するとクローン後に再生成が必要。
#   容量削減のためにこれらを除外することが多い。

# CubeMX file
*.ioc
# ※.iocファイルは重要なので、コミットする（除外しない）
